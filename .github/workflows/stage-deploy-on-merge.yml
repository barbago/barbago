name: Stage Deploy on Main
'on':
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  stage_deploy:
    name: Stage
    uses: ./.github/workflows/deploy.yml
    with:
      environment: stage
    secrets: inherit
